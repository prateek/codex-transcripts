# Notes

- Figure out where Codex stores session history + schema/versioning.
- Port claude-code-transcripts to Codex: parsing, redaction, output formats, and publishing.
- Add schema validation + fixtures to detect upstream format drift.
- TUI renderer inspiration: gemini-cli, pi-mono, opencode; implemented viewer using Textual Tree + filters.
